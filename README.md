# Neovim config
## using lazy as plugins manager.

```shell
if you want to copy item from nvim to system, you need to open opt.clipboard = "unnamedplus".
and install xclip to your system.
example(archlinux) :
sudo pacman -S xclip

the plugin -telescope dependency for system
sudo pacman -S ripgrep fd 
```
